class BookMyShow {
  constructor(n: number, m: number) {
    // Constructor function should construct a matrix?
    // Is there a better way to represent this than matrix?
  }

  gather(k: number, maxRow: number): number[] {
    return [];
  }

  scatter(k: number, maxRow: number): boolean {
    return true;
  }

  bookSeat() {

  }
}

/**
* Your BookMyShow object will be instantiated and called as such:
* var obj = new BookMyShow(n, m)
* var param_1 = obj.gather(k,maxRow)
* var param_2 = obj.scatter(k,maxRow)
*/

let currentShow = new BookMyShow(2, 5);